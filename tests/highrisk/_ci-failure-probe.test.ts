/**
 * ⚠️ 일회용 — CI 실패 검출 실증용. 이 커밋에서만 존재하고 다음 커밋에서 삭제된다.
 * 목적: "게이트가 실제로 빨간불을 내는가"를 초록불만으로 주장하지 않기 위함
 * (G2 §3-G — 게이트 라이브 판정은 트리거 경로로만).
 */
import { describe, it, expect } from "vitest";

describe("CI 실패 검출 실증", () => {
  it("의도적으로 실패한다", () => {
    expect(1).toBe(2);
  });
});
