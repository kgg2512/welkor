/**
 * ⚠️ 일회용 — CI **빌드 단계** 실패 검출 실증용. 이 커밋에서만 존재하고 다음 커밋에서 삭제된다.
 * 어디서도 import 되지 않으므로 테스트는 초록인 채 **빌드만** 깨진다 →
 * "빌드 단계가 실제로 빨간불을 내는가"를 분리해 증명한다(G2 §3-G).
 */
export const CI_BUILD_PROBE: number = "이 문자열은 number 가 아니다";
